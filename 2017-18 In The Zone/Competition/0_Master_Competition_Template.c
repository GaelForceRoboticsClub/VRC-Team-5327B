#pragma config(UART_Usage, UART1, uartVEXLCD, baudRate19200, IOPins, None, None)
#pragma config(UART_Usage, UART2, uartNotUsed, baudRate4800, IOPins, None, None)
#pragma config(Sensor, in1,    LineL,          sensorLineFollower)
#pragma config(Sensor, in2,    LiftPot,        sensorPotentiometer)
#pragma config(Sensor, in3,    LineR,          sensorLineFollower)
#pragma config(Sensor, in4,    MogoPot,        sensorPotentiometer)
#pragma config(Sensor, in5,    VbarPot,        sensorPotentiometer)
#pragma config(Sensor, dgtl1,  RBaseEnc,       sensorQuadEncoder)
#pragma config(Sensor, dgtl3,  LBaseEnc,       sensorQuadEncoder)
#pragma config(Sensor, dgtl9,  RollersEnc,     sensorQuadEncoder)
#pragma config(Sensor, dgtl12, MogoLimit,      sensorTouch)
#pragma config(Motor,  port2,           RFBase,        tmotorVex393HighSpeed_MC29, openLoop, reversed, driveRight)
#pragma config(Motor,  port3,           LFBase,        tmotorVex393HighSpeed_MC29, openLoop, reversed, driveLeft)
#pragma config(Motor,  port4,           LiftM,         tmotorVex393HighSpeed_MC29, openLoop, reversed)
#pragma config(Motor,  port5,           VbarM,         tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port6,           LBBase,        tmotorVex393HighSpeed_MC29, openLoop, reversed, driveLeft)
#pragma config(Motor,  port7,           RBBase,        tmotorVex393HighSpeed_MC29, openLoop, reversed, driveRight)
#pragma config(Motor,  port8,           RollersM,      tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port9,           MogoM,         tmotorVex393HighSpeed_MC29, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

//VRC-specific pragmas below
#pragma platform(VEX)
#pragma competitionControl(Competition)
#pragma autonomousDuration(20)
#pragma userControlDuration(120)
#include "Vex_Competition_Includes.c"
//VRC-specific pragmas above

//BCI library includes below
#define MENU_NUM 4
#include "Libraries/BCI/BCI.h"
//BCI library includes above

//Custom includes below
#include "Includes/Constants.h"
#include "Includes/JoystickDefs.h"
//#include "Includes/Lift.h"
#include "Includes/Mogo.h"
//#include "Includes/Vertibar.h"
#include "Includes/Base.h"
//Custom includes above

void pre_auton()
{
	//Ensure that tasks stop successfully
	bStopTasksBetweenModes = false;
}

void startDriverTasks()
{
	//Enable all of the various drivermode tasks
	//startTask(driverRollersTask);
	//startTask(driverVbarTask);
	//startTask(driverLiftTask);
	startTask(driverBaseTask);
	startTask(driverMogoTask);
}

task autonomous()
{
	//Run specific autonomous mode code here
	//a20pt1C();
}

task usercontrol()
{
	//Initiate the driver tasks for driver mode
	startDriverTasks();
	while(true)
	{
		//Keep this task running to avoid the competition switch
		//from thinking we have disconnected
		EndTimeSlice();
	}
}
