#pragma config(UART_Usage, UART1, uartVEXLCD, baudRate19200, IOPins, None, None)
#pragma config(UART_Usage, UART2, uartNotUsed, baudRate4800, IOPins, None, None)
#pragma config(Sensor, in1,    LiftPotL,       sensorPotentiometer)
#pragma config(Sensor, in2,    LiftPotR,       sensorPotentiometer)
#pragma config(Sensor, in4,    Gyro,           sensorGyro)
#pragma config(Sensor, in5,    LineL,          sensorLineFollower)
#pragma config(Sensor, in7,    LineR,          sensorLineFollower)
#pragma config(Sensor, in8,    MogoPot,        sensorPotentiometer)
#pragma config(Sensor, dgtl1,  LBaseEnc,       sensorQuadEncoder)
#pragma config(Sensor, dgtl3,  VbarP,          sensorDigitalOut)
#pragma config(Sensor, dgtl4,  IntakeP,        sensorDigitalOut)
#pragma config(Sensor, dgtl8,  BrakeP,         sensorDigitalOut)
#pragma config(Sensor, dgtl9,  MogoLimit,      sensorTouch)
#pragma config(Sensor, dgtl10, ActuationsLED,  sensorLEDtoVCC)
#pragma config(Sensor, dgtl11, RBaseEnc,       sensorQuadEncoder)
#pragma config(Motor,  port2,           RFDMD,         tmotorVex393HighSpeed_MC29, openLoop, reversed, driveRight)
#pragma config(Motor,  port3,           RBase,         tmotorVex393HighSpeed_MC29, openLoop, reversed, driveRight, encoderPort, dgtl11)
#pragma config(Motor,  port4,           RBDMD,         tmotorVex393HighSpeed_MC29, openLoop, reversed, driveRight)
#pragma config(Motor,  port5,           LiftR,         tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port6,           LiftL,         tmotorVex393HighSpeed_MC29, openLoop)
#pragma config(Motor,  port7,           LBDMD,         tmotorVex393HighSpeed_MC29, openLoop, reversed, driveLeft)
#pragma config(Motor,  port8,           LBase,         tmotorVex393HighSpeed_MC29, openLoop, driveLeft, encoderPort, dgtl1)
#pragma config(Motor,  port9,           LFDMD,         tmotorVex393HighSpeed_MC29, openLoop, reversed, driveLeft)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

//VRC-specific pragmas below
#pragma platform(VEX)
#pragma competitionControl(Competition)
#pragma autonomousDuration(20)
#pragma userControlDuration(120)
#include "Vex_Competition_Includes.c"
//VRC-specific pragmas above

//BCI library includes below
#define MENU_NUM 4
#include "Libraries/BCI/BCI.h"
//BCI library includes above

//Custom includes below
#include "Includes/Constants.h"
#include "Includes/JoystickDefs.h"
#include "Includes/Auxiliary.h"
#include "Includes/Base.h"
#include "Includes/Lift.h"
#include "Includes/Claw.h"
#include "Includes/Vertibar.h"
//Custom includes above

void pre_auton()
{
	//Ensure that tasks stop successfully
	bStopTasksBetweenModes = false;
}

void startDriverTasks()
{
	//Enable all of the various drivermode tasks
	startTask(driverClawTask);
	startTask(driverVbarTask);
	startTask(driverActuationMonitorTask);
	startTask(driverBrakeTask);
	startTask(driverLiftTask);
	startTask(driverBaseTask);
}

task autonomous()
{
	//Run specific autonomous mode code here
	//a20pt1C();
}

task usercontrol()
{
	//Initiate the driver tasks for driver mode
	startDriverTasks();
	while(true)
	{
		//Keep this task running to avoid the competition switch
		//from thinking we have disconnected
		EndTimeSlice();
	}
}
